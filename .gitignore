# OpenClaw config contains API keys — keep local only
openclaw-config.json5

# Dependencies
node_modules/
.env
*.csv
google-service-account.json
.wrangler/
.dev.vars
.dev.vars.*
.hermes/

# Reconciliation working files — real customer data, never commit.
# Ignore file CONTENTS but keep the two directories tracked via .gitkeep.
reports/inputs/*
reports/outputs/*
!reports/inputs/.gitkeep
!reports/outputs/.gitkeep
