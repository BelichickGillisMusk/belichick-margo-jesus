# OpenClaw config contains API keys — keep local only
openclaw-config.json5

# Dependencies
node_modules/
.env
*.csv
google-service-account.json
.wrangler/
.dev.vars
.dev.vars.*
.hermes/

# Kimi intel runtime — roster.json is committed; snapshots, events, briefs are not
intel/memory/*
!intel/memory/.gitkeep
intel/briefs/*
!intel/briefs/.gitkeep
